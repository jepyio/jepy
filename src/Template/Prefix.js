/**
 * @enum {String}
 */
const Prefix = {
    PARAMETER: '%',
};

/**
 * @enum {String}
 */
const BlockPrefix = {
    REPEATING: '#',
    CONDITIONAL: '?',
    TAB_INDENTED: '>',
    SPACE_INDENTED: '_',
    CACHED: '=',
};

export {Prefix, BlockPrefix};
